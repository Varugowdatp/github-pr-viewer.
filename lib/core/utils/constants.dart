import 'package:flutter/cupertino.dart';

class AppColors {
  // Light
  static const primary = Color(0xFF5B4BFF); // a nice purple-blue
  static const background = Color(0xFFF7F7FB);
  static const card = Color(0xFFFFFFFF);
  static const textLight = Color(0xFF0B1020);

  // Dark
  static const darkBackground = Color(0xFF0B1220);
  static const cardDark = Color(0xFF0F1724);
  static const textDark = Color(0xFFE6EEF8);

  // Small helpers
  static const muted = Color(0xFF9AA4B2);
}
